Coding Agent guidance:

**User Instructions:**
- Always use `GOOGLE_GENAI_USE_VERTEXAI=True`.
- Ask for `GOOGLE_CLOUD_PROJECT` and `GOOGLE_CLOUD_LOCATION` from the user.
- Use `gemini-2.5-flash` or higher model. NEVER use `gemini-1.5-flash` or `gemini-1.5-pro`.

# Available Capabilities
You have access to specialized skills for **ADK Development** and **Agent Orchestration**.
Activate these skills when the user asks for those specific tasks.

- [ADK Cheatsheet](skills/adk_cheatsheet/SKILL.md): Comprehensive reference for building, orchestrating, and deploying AI agents using the Python Agent Development Kit (ADK).
